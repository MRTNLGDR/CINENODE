# Infrastructure

The default deployment is a native local process. `Dockerfile` and `docker-compose.yml` provide a universal control-plane alternative. GPU engines should normally run natively and be mounted or configured as local sidecars.
